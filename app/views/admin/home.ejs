<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title> Home </title>
    
    <%- include ('layouts/head') %>

</head>
<body>
    <div class="container">
    <%- include ('layouts/nav') %>

        <div class="row" style="margin-bottom: 50xp;">
            <div class="col-md-12">
                <a href="/admin/post/new" class = "btn btn-success pull-right"> Add new posts</a>
    
            </div>
        </div>

        <div class="panel panel-default">
            <div class="panel-heading">
                <h1>Posts</h1>

                <div id ="signupalert" style = " <% if (!data || !data.error) { %> display:none <% } %> " class = "alert alert-danger">
                    <p>Error:</p>
                    <span><% if (data && data.error) { %> <%= data.error %> <% } %></span>
                </div>  

                <div class = "panel-body">
                    <div class = "table-responsive">
                        <table class="table table-striped table-bodered table-holder" >
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Title</th>
                                    <th style="width: 900px;">Content</th>
                                    <th>Author</th>
                                    <th>Created At</th>
                                    <th>Updated At</th>
                                    <th>Action </th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if(data && data.posts){%>
                                   <% for (let i = 0; i < data.posts.length; i++) {%>
                                        <tr>
                                            <td><%= data.posts[i].id %></td>
                                            <td><%= data.posts[i].title %></td>
                                            <td><%= data.posts[i].content %></td>
                                            <td><%= data.posts[i].author %></td>
                                            <td><%= data.posts[i].created_at %></td>
                                            <td><%= data.posts[i].updated_at %></td>
                                            <td>
                                                <a href="/admin/post/edit/<%= data.posts[i].id %>" class="btn btn-primary">Edit</a>
                                                <a href="/admin/post/delete/<%=data.posts[i].id %>" class="btn btn-danger">Delete</a>
                                            </td>
                                        </tr>
                                    <%}%>
                               <% }%>

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    
</body>
</html>